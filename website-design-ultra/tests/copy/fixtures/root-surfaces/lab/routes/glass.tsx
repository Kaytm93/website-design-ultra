// A lab route carries no marketing copy by design (TODO.md T2.1). The
// root-surface discovery declares this surface structurally instead of
// linting it into a NO-COPY verdict.
export const glass = { uniforms: { uTime: 0 } }
